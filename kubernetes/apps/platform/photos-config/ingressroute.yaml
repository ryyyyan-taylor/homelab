apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: photos
  namespace: photos
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`photos.lab.ryantaylor.tech`)
      kind: Rule
      services:
        - name: immich-server
          port: 2283
  tls: {}
