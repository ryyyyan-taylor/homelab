apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: photos-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "27"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/ryyyyan-taylor/homelab.git
    targetRevision: HEAD
    path: kubernetes/apps/platform/photos-config
  destination:
    server: https://kubernetes.default.svc
    namespace: photos
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
