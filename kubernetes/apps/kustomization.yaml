apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Platform Application resources are added here as Phase 1 continues.
# Each entry is an Argo CD Application CRD pointing at a path under kubernetes/apps/.
