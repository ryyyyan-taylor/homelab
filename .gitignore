# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars

# Ansible
*.retry
.ansible/
ansible/inventory/proxmox.yml

# Age private keys — never commit
*.age

# Unencrypted secret staging files — must be SOPS-encrypted before committing
*-secrets.yaml
*-secret.yaml

# Talos machine configs — contain bootstrap secrets
controlplane.yaml
worker.yaml
talosconfig

# OS
.DS_Store
Thumbs.db

# Claude Code & local planning
CLAUDE.md
PLAN.md
